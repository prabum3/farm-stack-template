version: '3.7'
services:
  nginx-reverse-proxy:
    image: {{cookiecutter.container_registry}}/nginx-react-image:latest
    ports:
      - 80:80
    depends_on:
      - "backend"

  backend:
    image: {{cookiecutter.container_registry}}/fastapi-image:latest
    expose:
     - "8000"

